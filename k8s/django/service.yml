apiVersion: v1
kind: Service
metadata:
  name: django-service
  namespace: emenu
  labels:
    app: django-svc
spec:
  type: ClusterIP
  selector:
    app: django
  ports:
  - port: 8000
    targetPort: 8000
